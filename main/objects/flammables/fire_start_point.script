function init(self)
	self.offset = go.get_position()
end

function update(self, dt)
	-- local pos = go.get_position()
	-- local root_pos = go.get_position("root")
	-- go.set_position(vmath.vector3(root_pos.x + self.offset.x, root_pos.y + self.offset.y, pos.z))
	-- print(go.get_position())
end